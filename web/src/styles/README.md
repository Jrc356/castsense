# Styles — Reference

`src/styles/` contains shared CSS custom properties (tokens) and base theme utilities used across screens and components.

Global styles are loaded via `src/index.css` and `src/App.css`. Component-level styling uses standard CSS class selectors. There is no CSS Modules or styled-components setup.

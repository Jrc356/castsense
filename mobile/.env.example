# Backend API Configuration
# Leave empty to auto-detect (localhost for simulator, LAN IP for physical device)
# Set explicitly for cloud/staging environments
API_BASE_URL=

# Environment
NODE_ENV=development
